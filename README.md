# Multi-Orders-in-Different-Loops
This query identifies multi‑orders split across different buffers. When destinations don’t match, the order is divided, increasing shipping costs since items are sent separately instead of in one box.
